apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: error-rate
spec:
  args:
  - name: service-name
  metrics:
  - name: error-rate
    interval: 2m
    successCondition: result <= 5
    failureLimit: 1
    provider:
      datadog:
        interval: 2m
        query: |
          sum:trace.express.request.hits.by_http_status{http.status_code:404,service:{{args.service-name}}}